extends ../layout

block content
        form(action="/patchuserpassword/" method="POST")
            input(type="hidden" name="id" value=id required)
            input(type="password" name="password" placeholder="password" required)
            input(type="submit" value="submit")